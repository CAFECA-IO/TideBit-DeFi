import React from 'react';
import TickerSelector from '../ticker_selector/ticker_selector';

const TradingHeader = () => {
  return (
    <div>
      TradingHeader
      <TickerSelector />
    </div>
  );
};

export default TradingHeader;
